#include <m68k.h>
#include <ram.h>

void step_processor(){
    // Take the opcode off of the current PC

}

void IR_WRITE(u32 reg, u8 dest, int latency, u32 bit_enable){

}
void FPR_WRITE(fp32 reg, u8 dest, int latency, u32 bit_enable){

}